import React, { useContext } from 'react';
import { AuthContext } from '../AuthProvider';

const Home = () => {
    return (
        <div>
            <p>I am Home page</p>
        </div>
    );
};

export default Home;