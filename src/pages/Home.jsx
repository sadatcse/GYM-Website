import React, { useContext } from 'react';
import Banner from '../components/Banner';

const Home = () => {
    return (
        <div>
            <Banner/>
        </div>
    );
};

export default Home;